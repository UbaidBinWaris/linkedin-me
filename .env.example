# ─────────────────────────────────────────────────────────
#  LinkedIn Comment Bot – Environment Config Template
#  Copy this file to .env and fill in your values.
# ─────────────────────────────────────────────────────────

# ── Gemini AI (required) ──────────────────────────────────
GEMINI_API_KEY=your_gemini_api_key_here

# ── LinkedIn credentials ──────────────────────────────────
# Used only to auto-fill the login form on first run.
# After the first login, the session is saved and these aren't used.
LINKEDIN_EMAIL=your_email@example.com
LINKEDIN_PASSWORD=your_password_here

# ── Bot behavior ──────────────────────────────────────────
# Maximum number of comments to post per run
MAX_COMMENTS_PER_RUN=10

# Min/max delay in milliseconds between actions (human-like pacing)
MIN_DELAY_MS=3000
MAX_DELAY_MS=8000

# Interest score threshold (0-100). Posts scoring below this are skipped.
# Higher = more selective. Lower = comments on more posts.
# Recommended: 40-60
MIN_INTEREST_SCORE=40

# ── Browser ───────────────────────────────────────────────
# false = visible window (recommended), true = headless background
HEADLESS=false
SESSION_DIR=./session

# ── Scheduling ────────────────────────────────────────────
# Timezone for the schedule (IANA timezone name)
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
SCHEDULE_TIMEZONE=Asia/Karachi

# Allowed hours (24h format). Bot will SKIP if run outside this window.
SCHEDULE_START_HOUR=9
SCHEDULE_END_HOUR=22

# Active days: 0=Sunday 1=Monday 2=Tuesday 3=Wednesday 4=Thursday 5=Friday 6=Saturday
# Default: Monday–Saturday (skip Sunday)
SCHEDULE_ACTIVE_DAYS=1,2,3,4,5,6

# ── Your LinkedIn Profile (used in Gemini prompt) ─────────
# The bot writes comments in your voice. Update these to match your profile.
MY_NAME=Ubaid Waris
MY_HEADLINE=Full Stack Developer | Next.js | Node.js | React | DevOps
MY_ABOUT=I build scalable web applications and automation tools. Passionate about clean code, developer productivity, and emerging technologies.
