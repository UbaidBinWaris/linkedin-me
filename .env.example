# ─────────────────────────────────────────
#  LinkedIn Comment Bot – Environment Config
# ─────────────────────────────────────────

# Your Gemini API Key (required)
GEMINI_API_KEY=your_gemini_api_key_here

# LinkedIn credentials (used only if auto-login is enabled)
LINKEDIN_EMAIL=your_email@example.com
LINKEDIN_PASSWORD=your_password_here

# Bot behavior
MAX_COMMENTS_PER_RUN=10
MIN_DELAY_MS=3000
MAX_DELAY_MS=8000

# Set to "true" to run browser in background (headless)
# Set to "false" to see the browser window (recommended for first run)
HEADLESS=false

# Session directory (where browser cookies/storage are saved)
SESSION_DIR=./session
